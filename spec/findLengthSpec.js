describe('FIND LENGTH', function(){
    it('should return length of the array given', function(){
        var arr = [1, 2, 3, 4]
        var result = lengthOfSequence(arr)
        expect(result).toEqual(4)
    })
})